# webnovel_rules 第二轮技术评审报告（R34b）

- 评审基线：`35e43e6 R34b`
- 工作树状态：干净，无未提交改动
- 测试基线：`59 passed, 1 skipped in 1.85s`（`venv/bin/python -m pytest tests/ -q`）
- 上一轮基线：R29，评分 6.0/10

---

## 1. 总体结论

### 综合评分：6.3 / 10（上轮 6.0）

分项对比：

| 维度 | 上轮 | 本轮 | 变化 |
|---|---|---|---|
| 功能完整度 | 8.5 | 9.0 | 上升 |
| 架构方向 | 7.5 | 7.0 | 下降 |
| 网络稳定性 | 6.5 | 7.5 | 上升 |
| 数据可靠性 | 5.5 | 7.0 | 上升 |
| 可维护性 | 5.5 | 4.5 | 下降 |
| 安全性 | 3.5 | 5.0 | 上升 |
| 工程化成熟度 | 5.5 | 5.0 | 下降 |

### 一句话结论

> R29 的安全与可靠性内核**基本保住了**，但一周内 app.py 从 938 行涨到 3722 行（约 4 倍），
> 新增功能几乎全部无测试保护，并且引入了 2 个**静默失效**的 P0 缺陷。
> 当前问题已从"地基不牢"转为"地基还行、上层盖太快"。

---

## 2. 上轮整改项留存复核

| 项 | 当前状态 | 证据 |
|---|---|---|
| Fetcher 会话租约 | 完整保留 | `engine/fetcher.py:73`、`:409`、`:671` |
| 业务级 deadline | 完整保留 | `engine/fetcher.py:25`、`:549`、`:614` |
| `retries=0` 首次请求 | 完整保留 | `engine/fetcher.py:587` |
| UID 路径穿越防护 | 完整保留 | `engine/source_mgr.py:17-40` |
| 封面 XSS 转义 | 完整保留 | `templates/manga.html:390-392` |
| 全局错误脱敏 | 被局部 except 削弱 | `app.py:3653-3674` vs `:2015/:2171/:2884/:3586/:3639` |
| SSRF 防护 | 被削弱，漏 1 个接口 | 定义 `app.py:3406-3443`，遗漏点见 P0-3 |
| 默认监听 127.0.0.1 | **已主动回退** | `app.py:3699` |

R33c 把 `--host` 默认改回 `0.0.0.0`，属于产品决策而非缺陷。
但它使"默认开放监听 + 默认无鉴权"重新成为出厂状态，需重新定级。

---

## 3. P0 级问题（必须优先修）

### P0-1 `_browser` 缺少 global 声明，Playwright 自愈完全失效

`engine/manga/copymanga_web.py:35` 只声明了 `global _pw_exec`，但 `:50-51` 赋值了 `_browser`：

```python
00035|     global _pw_exec
...
00050|             with _browser_lock:
00051|                 _browser = None
```

`_browser` 因此变成 `_run_pw` 的局部变量，模块级 `_browser`（`:19`）**从未被清空**。

后果：worker 线程崩溃后重建了 `_pw_exec`，但 `_get_page()`（`:62-69`）仍复用属于已死线程的旧 context，
重试会立刻再次抛 `cannot switch to a different thread`，退化为永久失败。
注释里写的"重建执行器并重试一次"实际不成立。

修复：在 `:35` 改为 `global _pw_exec, _browser`。

### P0-2 两个方法缩进错位，落在模块函数体内且位于 return 之后

`engine/manga/copymanga_web.py:452` 是模块级函数 `_app_chapter2_images`，其 `return imgs` 在 `:497`。
紧接着：

```python
00497|     return imgs
00498|
00499|     def image_headers(self, image_url):
00500|         # 图片防盗链：Referer 用网页版站点
00501|         return {"User-Agent": UA, "Referer": WEB + "/"}
00502|
00503|     def image_url(self, image_url):
00504|         return image_url
```

这两个 `def` 缩进在模块函数内部，且在 `return` 之后，**永不执行**，也**不是 `CopyMangaWeb` 的方法**。

后果：`CopyMangaWeb` 回落到基类 `engine/manga/base.py:83-85`，只返回 UA、**不带 Referer**，
与文件头 `:6` 自述的"图片下载：curl_cffi + Referer 防盗链"矛盾，图片易被 403。

修复：将这两个方法移入 `CopyMangaWeb` 类体并改为 4 空格类内缩进。

### P0-3 `/api/search-detail` 遗漏 SSRF 校验

`app.py:2136-2147`：

```python
02141|     book_url = data.get("book_url")
02143|     if not src or not book_url:
02144|         return jsonify({"error": "参数不完整"}), 400
02145|     try:
02146|         c = SourceCrawler(src)
02147|         book = c.get_book(book_url, fast=True)
```

同结构的 `/api/search-toc`（`app.py:2029`）和 `/api/search-toc-count`（`:1963`）都调用了 `_safe_target_url`，
唯独此处遗漏，属实现疏漏而非设计取舍。

叠加 `app.py:2171` 把原始异常文本回传，构成**有回显的 SSRF**，可用于探测内网端口与 Banner。

修复：`:2145` 前补 `book_url = _safe_target_url(book_url, "书籍URL")`。

---

## 4. P1 级问题

### P1-1 默认 `0.0.0.0` + 默认无鉴权

`app.py:3699` 默认开放监听，`app.py:3710-3712` 仅打印一行警告。
鉴权链路本身实现质量不错（`app.py:545-558` 加载、`:599-607` 拦截、`:572-594` 爆破退避），
但是 opt-in，且当前环境 `data/auth.json` 不存在。

无凭据可达的破坏性写操作包括：

| 端点 | 行号 |
|---|---|
| `DELETE /api/books/<book_key>` | `app.py:1927` |
| `DELETE /api/manga/library/<source>/<comic_id>` | `app.py:3143` |
| `DELETE /api/sources/<uid>` | `app.py:778` |
| `POST /api/cache/clean`（scope=all 连 trash 清空） | `app.py:1783-1788` |
| `GET /api/manga/history` / `favorites` | `app.py:3446`、`:3479` |

放大因素：无 CSRF token、无 Origin/Referer 校验；登录 cookie（`app.py:586`）未设 `samesite`。

家庭局域网实际风险：**中高**。
建议折中方案：未配置鉴权时默认 `127.0.0.1`，配置鉴权后默认 `0.0.0.0`，
兼顾 R33c 的手机端可用性诉求与安全基线。

### P1-2 R34 分页 / total_hits 三层语义不一致

- `app.py:2589-2594` 多源取 `max(total)`，得到的是"某一个源"的总数；
- 前端 `templates/manga.html:265`、`:334` 用 `page * 30 < totalHits` 反推是否还有下一页；
- 但多源单页最多可返回 `30 × 源数` 条，`jm.py:171-173` 从不设置 total，
  且 `app.py:2552` 的 20s `wait` 会让走 Playwright 的 copymanga_web 几乎必然被丢弃。

结果：多源、jm、末页、聚合去重四种情况下判定全部失准。
页大小 30 还硬编码在四处（`copymanga.py:391`、`copymanga_web.py:218/223`、`manga.html:265/334`）。

建议服务端改返回 `{page_size, raw_count, per_source_total}`，前端不再自行推算。

### P1-3 翻页越界会清空结果区

`templates/manga.html:256-258` 先无条件清空卡片，`:259` 才判断 `list.length`。
越界页返回空列表时，旧卡片已删、新卡片为 0、`SEARCH.page` 不回退，页面变白。
应改为"取到数据后再替换渲染"。

### P1-4 三个排序字段"写而不读"，其中一个带重复目录遍历

- `first_dl_ts`（`app.py:1593`）：每次 `/api/books` 对每本书 `os.listdir` + 逐个 `.cache` 做 `getmtime`（`:1574-1576`），
  而 `:1550-1553` 已经遍历过同一目录；R32 已下线"最早下载"排序档，无任何消费方。
- `created_ts`（`app.py:1592`）：依赖 `st_birthtime`，Android/Linux 退化为 inode 变更时间，语义失真。
- `first_downloaded_at`（`engine/manga/download_manager.py:552`）：无消费方，
  且 `:543` 用字符串比较判断时间先后，时钟回拨时会重置"首次时间"。

千章书 × N 本时，这是每次书库刷新数万次多余 syscall。

---

## 5. P2 级问题

- **Playwright evaluate 字符串拼接**：`copymanga_web.py:210` 仅转义单引号，反斜杠/换行/`\u2028` 未处理，
  拼入 JS 源码（`:221-226`），存在页面上下文 JS 注入面。应改用 `page.evaluate(fn, arg)` 传参。
- **`TypeError` 被当作能力探测**：`app.py:2534-2537` 用 `except TypeError` 判断适配器是否支持 `order`，
  若适配器内部抛 `TypeError` 会误触发**第二次完整搜索**（copymanga_web 即再跑一轮 Playwright）。
  应改用 `inspect.signature` 或统一基类签名。
- **`chapter_id` 缺路径段校验**：`app.py:3592-3593` 校验了 `source` 和 `comic_id`，
  但 `:3611` 直接把未校验的 `chapter_id` 拼入路径并 `os.makedirs`。同类见 `:2954-2962`。
- **`_safe_uid` 旁路写入点**：`app.py:759-764` 自行拼 `sources/{uid}.json` 而未走 `_source_path`，
  当前不可直接利用，但属 defense-in-depth 缺口。
- **禁漫码直达边界**：`templates/manga.html:297-308` 忽略用户选定书源、位数硬编码 6-7、
  无存在性校验、仅非 silent 分支生效；服务端 `app.py:706-712` 无 source/id 白名单。
- **双份代码树**：`android_app/app/src/main/python/` 下存在行号一致的镜像副本，
  本轮新增逻辑全靠复制粘贴同步，无构建期机制，必然漂移。

---

## 6. 工程化评估

### 测试覆盖盲区

- app.py 共 67 个路由，约 23 个被触达，覆盖率约 **34%**。
- **漫画板块 28 个路由 100% 零覆盖**，含 R34/R34b 刚重写的 `/api/manga/search`（`app.py:2496`）
  和 R29 的 SSRF 防护点 `/api/manga/.../proxy`（`:3589`）。
- `engine/manga/download_manager.py`（610 行）仅 2/29 函数被测，
  `_worker`（`:296`，219 行并发核心）、`pause/resume/cancel`（`:217-249`）、`_write_library`（`:515`）全零。
- `engine/source_mgr.py`（258 行）0/13 函数——**安全加固代码本身零测试**。
- `engine/search_service.py`（186 行）0/12 函数。
- `engine/fetcher.py` 仅 3 个测试（均为上轮新增），
  其自述的两大核心能力**封锁检测**（`_looks_blocked:509`、`_on_block:496`）
  和**代理池**（`_pick_proxy:207`、`_load_proxy_pool:279`）恰好是零测试区。

### 基建缺口（全部确认不存在）

- 无 CI（无 `.github/`）——257 次 commit 零自动化验证
- 无 lockfile
- 无 `pyproject.toml` / `pytest.ini` / `conftest.py`
- 无 pre-commit、无覆盖率工具

依赖不一致 3 处：

1. `pytest-timeout>=2.3` 已声明但 venv 未安装，且 `tests/` 无任何 `@pytest.mark.timeout`；
2. `beautifulsoup4` 已装但任何 requirements 文件均未声明（幽灵依赖）；
3. 全部为 `>=` 下限约束且无 lockfile，已实际漂移到 pytest 9.1.1 / lxml 6.1.2 / pillow 12.3.0。

### 体量增长

app.py 七天增长曲线：

| 日期 | commit | 行数 |
|---|---|---|
| 08-20 | ea6edda | 938 |
| 08-21 | cefae8f | 2084 |
| 08-23 | 2901143 | 3028 |
| 08-27 | 35e43e6 | **3722** |

**7 天 3.97 倍，日均 +398 行，无收敛迹象**，同期测试仅 57 个用例。
这是典型的"单体主控文件 + 手测驱动"技术债累积形态。

---

## 7. 建议整改顺序

### 第一批（当天，均为小改动）

1. `copymanga_web.py:35` 补 `global _browser`
2. `copymanga_web.py:499-504` 两个方法移入 `CopyMangaWeb` 类体
3. `app.py:2145` 补 `_safe_target_url`
4. `app.py:3592` 后补 `chapter_id = _safe_seg(chapter_id, "章节")`
5. `app.py:586` 补 `samesite="Lax"`

### 第二批（本周）

6. 建立最小 CI：跑 pytest + py_compile
7. `pip freeze > requirements.lock`，清理幽灵依赖
8. 移除 `app.py:2015/2171/2884/3586/3639` 的原始异常回传
9. 删除 `first_dl_ts` / `created_ts` / `first_downloaded_at` 三个死字段及其目录遍历
10. 修复分页越界清空结果区

### 第三批（两周内）

11. 重构 total_hits 契约为 `{page_size, raw_count, per_source_total}`
12. 抽 `conftest.py`，补漫画 API 冒烟测试与 download_manager 并发测试
13. 补 fetcher 封锁/代理/限速单测
14. 补 `source_mgr._safe_uid` 安全回归测试

### 第四批（排期）

15. 按 Blueprint 拆分 app.py，漫画侧 28 路由（约 1140 行）是最自然的第一刀
16. 建立 Android 副本的构建期同步机制，消除双份代码树
17. 引入 pytest-cov 并设覆盖率阈值
