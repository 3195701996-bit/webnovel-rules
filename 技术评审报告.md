# webnovel_rules 爬虫系统技术评审报告

## 1. 评审结论

### 总评：功能成熟度较高，生产工程成熟度中等，当前不建议直接暴露到不可信网络

这不是一个简单的单站点脚本，而是一套由以下部分组成的复合系统：

- Flask 局域网服务与 Web UI；
- 基于 legado 规则子集的通用书源引擎；
- 多站点小说适配器；
- 漫画多源适配器与下载管理器；
- JSON 文件持久化、缓存、断点续爬和后台任务；
- Android/Chaquopy 客户端封装。

系统的核心设计方向是正确的：规则引擎负责通用解析，专属适配器处理复杂站点，Fetcher 负责 HTTP、限流和反爬状态，任务层负责断点与重试。这使系统具备较好的功能广度和站点扩展能力。

但从高端工程视角看，当前主要问题集中在三个方面：

1. **安全边界默认不成立**：鉴权是可选且默认关闭，服务默认监听 `0.0.0.0`；多个接口对用户提供的 URL 缺少目标域限制，书源 UID 还存在路径穿越写入风险。
2. **可靠性保护没有覆盖最关键的持久化路径**：断点状态、章节缓存、漫画任务状态和最终 TXT 文件存在非原子写入；部分任务线程异常还可能留下永久 `running` 状态。
3. **架构已出现明显演进债务**：`app.py` 超过 3500 行，路由、任务编排、缓存、搜索聚合、漫画逻辑和鉴权集中在同一文件，重复实现和私有成员直连增加了回归风险。

因此，本系统适合作为个人/家庭局域网使用的功能型工具，但在修复安全和可靠性问题前，不宜作为公网服务、多人服务或无人值守生产系统运行。

---

## 2. 评分矩阵

| 维度 | 评分 | 判断 |
|---|---:|---|
| 功能完整度 | 8.5/10 | 小说、漫画、搜索、下载、断点、阅读、书源管理覆盖较全 |
| 核心架构方向 | 7.5/10 | 规则引擎、适配器、Fetcher 分层方向正确 |
| 扩展性 | 7/10 | 新增书源较方便；漫画源注册仍依赖 `app.py` 硬编码 |
| 网络稳定性 | 6.5/10 | 有超时、重试、限流、封锁检测，但状态重建和总超时仍有风险 |
| 数据可靠性 | 5.5/10 | 有断点和原子写工具，但关键下载状态没有统一使用原子写 |
| 安全性 | 3.5/10 | 默认暴露、SSRF、路径穿越、XSS 和明文签名口令问题需要优先修复 |
| 可维护性 | 5.5/10 | 单文件过载、重复逻辑、私有成员耦合明显 |
| 测试与交付 | 5.5/10 | 离线 API 测试可用，但核心稳定性和安全路径覆盖不足 |
| **综合** | **6.0/10** | **功能型个人系统；尚未达到生产级服务标准** |

评分是基于当前代码结构、测试结果和静态风险的工程判断，不代表线上成功率或具体站点可用率。

---

## 3. 架构评估

### 3.1 当前主要调用链

小说搜索大致为：

```text
Flask API
  -> SearchWorker
  -> SourceCrawler.search
  -> 专属 Adapter 或 RuleEngine
  -> Fetcher
  -> 目标站点
```

小说整书下载大致为：

```text
创建任务
  -> 后台线程 _run_task
  -> CrawlTask.crawl
  -> 获取详情 / 目录 / 正文
  -> 章节缓存
  -> merge_txt
```

漫画路径大致为：

```text
Flask 漫画 API
  -> MangaManager / MangaAdapter
  -> 详情或章节获取
  -> ImageDownloader
  -> DownloadManager
  -> 本地缓存和书库
```

主要入口与调用链可见 [`app.py`](app.py)、[`engine/crawler.py`](engine/crawler.py)、[`engine/search_service.py`](engine/search_service.py)、[`engine/manga/manager.py`](engine/manga/manager.py)。

### 3.2 做得好的地方

#### A. 通用规则与站点适配器双轨设计合理

[`engine/rules.py`](engine/rules.py) 将 CSS、XPath、JSONPath、正则、模板替换和文本清洗封装为规则能力；复杂站点再通过 `engine/adapters/` 定制，避免所有站点逻辑堆在一个解析器中。

#### B. Fetcher 已经具备工程化雏形

[`engine/fetcher.py`](engine/fetcher.py) 中已经考虑：

- 请求超时与有限重试；
- 403/412/418/429/503 等封锁状态；
- `Retry-After` 与指数退避；
- 每域名限流；
- UA 轮换；
- 会话重建；
- 代理池；
- 慢源和封锁状态追踪。

这比常见的“requests + for 循环”脚本高一个层级。

#### C. 漫画适配器抽象清晰

[`engine/manga/base.py`](engine/manga/base.py) 定义了 `MangaAdapter`、`Comic`、`Chapter`、`ComicDetails` 等抽象；[`engine/manga/manager.py`](engine/manga/manager.py) 使用注册表管理适配器。新增漫画源的核心工作量主要集中在适配器内部，方向正确。

#### D. 任务有暂停、恢复、失败重试和断点意识

[`engine/crawler.py:564`](engine/crawler.py:564) 起的 `CrawlTask.crawl()` 已考虑：

- 失败章节优先重试；
- 并发下载；
- 停止回调；
- 章节缓存复用；
- 失败章节落盘；
- 断点恢复。

这些能力说明系统不是一次性抓取脚本，而是向任务型应用演进。

### 3.3 架构主要问题

#### A. `app.py` 已成为 God File

[`app.py`](app.py) 超过 3500 行，同时承载：

- Flask 路由；
- 小说任务调度；
- 搜索聚合和排序；
- TOC 缓存；
- 书库管理；
- 漫画 API；
- 鉴权；
- 自动清理；
- 后台健康检查。

建议至少拆成：

```text
app/
  routes/novel.py
  routes/manga.py
  routes/tasks.py
  routes/auth.py
  services/search_service.py
  services/task_service.py
  services/library_service.py
  repositories/json_repository.py
  security/url_policy.py
```

#### B. 多份事实源和重复逻辑

已发现以下重复或分叉：

- `app.py` 与 `engine/config.py` 都定义任务状态；
- 缓存路径和原子写逻辑存在多处实现；
- 搜索分组/清洗逻辑在普通搜索和流式搜索中重复；
- `search-toc` 与 `search-toc-count` 各自实现单飞、缓存、目录抓取；
- 漫画详情的章节拆分与排序存在多处复制。

这种结构短期开发速度快，长期会导致“修了 A 路径，B 路径仍然有旧逻辑”。

#### C. 跨模块访问私有状态

例如 `app.py` 直接读取搜索跟踪器的 `_slow`、`_latency`，并对适配器内部限流状态打补丁。私有属性一旦改名或改变结构，外部模块会静默损坏。

建议提供明确接口，例如：

```python
slow_tracker.snapshot()
adapter.reset_rate_limit()
adapter.health_status()
```

#### D. 小说任务和漫画任务是两套状态机

小说任务使用 `app.py` 全局字典 `_tasks/_crawlers`；漫画任务由 `DownloadManager` 管理。两套任务的暂停、停止、删除、持久化和恢复语义不同，前端却将它们合并展示，长期容易产生状态不一致。

建议抽象统一的 `TaskStore` 和 `TaskLifecycle`，至少统一：

- `pending/running/paused/stopped/failed/completed` 状态；
- 任务取消语义；
- 恢复策略；
- 进度事件；
- 持久化格式。

---

## 4. 最高优先级问题

## P0/P1：必须先修

### 4.1 默认鉴权关闭且默认监听所有网卡

证据：

- [`app.py:499-518`](app.py:499) 只有设置 `WR_AUTH_PASSWORD` 或存在 `data/auth.json` 时才启用鉴权；
- [`app.py:3514`](app.py:3514) 默认 host 为 `0.0.0.0`；
- [`app.py:559-567`](app.py:559) 在鉴权关闭时放行全部请求；
- README 也明确把鉴权描述为“可选”，见 [`README.md:99-104`](README.md:99)。

影响：局域网其他设备可以读取书库、访问阅读内容、创建爬取任务、删除数据，且可利用下面的 URL 代拉接口。

建议：

1. 默认绑定 `127.0.0.1`，只有显式 `--host 0.0.0.0` 才开放局域网；
2. 首次启动强制生成随机管理员密码，或首次访问必须完成初始化；
3. 所有写操作必须鉴权；
4. 生产模式禁止 `--debug`；
5. 增加 CSRF token 或对所有写 API 使用独立 bearer token。

### 4.2 多个接口构成 SSRF

多个接口只做了 `startswith("http")` 检查：

- 任务创建：[`app.py:1396-1408`](app.py:1396)；
- 搜索目录计数：[`app.py:1877-1935`](app.py:1877)；
- 搜索目录：[`app.py:1938-1999`](app.py:1938)；
- 漫画图片代理：[`app.py:3425-3472`](app.py:3425)。

Fetcher 最终会直接访问传入 URL，见 [`engine/fetcher.py`](engine/fetcher.py)。

风险包括：

- 访问回环地址和内网服务；
- 访问云环境元数据地址；
- 借助服务端代理探测内部端口；
- 将请求头和流量转发到非预期目标。

建议集中实现 `URLPolicy`：

- 解析 URL 后只允许 `http/https`；
- 禁止 `localhost`、回环、链路本地、私有网段、保留地址和 Unix socket 映射；
- 任务、目录、详情和图片代理统一复用；
- 默认只允许目标 URL 与书源 `bookSourceUrl` 同源或同注册域；
- 代理接口只允许来自已验证漫画适配器返回的 URL，禁止客户端任意覆盖。

### 4.3 书源 UID 未净化导致路径穿越写入

证据：

- [`engine/source_mgr.py:16-17`](engine/source_mgr.py:16) 直接拼接 `f"{uid}.json"`；
- [`engine/source_mgr.py:107-116`](engine/source_mgr.py:107) 优先使用用户传入的 `uid` 并直接落盘；
- `/api/sources/import` 是外部入口。

攻击者可构造包含 `../` 的 UID，使目标路径逃出 `sources/` 目录，覆盖其他 JSON 文件。尤其危险的是，缺少 `searchUrl` 的源在 [`engine/source_mgr.py:197-200`](engine/source_mgr.py:197) 可直接视为校验通过，因此可能绕过网络校验完成写入。

建议：

- UID 只允许 `[A-Za-z0-9._-]`，长度限制在合理范围；
- 服务端永远基于规范化 URL/随机 UUID 生成 UID，不接受客户端 UID；
- 写入前使用 `realpath` 校验目标路径必须位于 `SOURCES_DIR` 内；
- 删除、读取和更新接口同样统一走安全路径函数；
- 增加路径穿越回归测试。

### 4.4 任务线程异常可能留下永久 running 状态

[`app.py:216-262`](app.py:216) 中先调用 `task.crawl()`，而外围 `try` 从 [`app.py:263`](app.py:263) 才开始。如果 `task.crawl()` 在进入 `try` 前抛异常，后台 daemon 线程会结束，但任务状态可能仍保持 `running`，`_crawlers` 也可能不清理。

影响：

- 任务无法正常暂停或删除；
- 重启前后任务状态不一致；
- 同书重复任务判断被永久阻塞。

建议将整个任务生命周期放入 `try/except/finally`：

```python
try:
    task.crawl(...)
    task.merge_txt()
except Exception as exc:
    mark_failed(task_id, exc)
finally:
    unregister_crawler(task_id)
```

同时增加任务 watchdog：发现 `running` 任务长时间无心跳时自动转为 `failed` 或 `stopped`。

---

## 5. 稳定性与数据可靠性

### 5.1 断点状态和章节缓存不是原子写

[`engine/crawler.py:491-505`](engine/crawler.py:491) 直接覆盖 `_state.json` 和 `_chapters.json`；[`engine/crawler.py:549-551`](engine/crawler.py:549) 直接写章节 `.cache`。

进程崩溃、断电或强制终止可能造成截断 JSON 或半写缓存。加载端会在 [`engine/crawler.py:507-523`](engine/crawler.py:507) 捕获异常并返回失败，结果可能表现为“断点消失”，而不是显式告警。

更严重的是，`_has_cache()` 主要依据文件存在判断已完成，损坏缓存可能被当成完成内容。

建议统一使用已有的 `atomic_write`：

- 临时文件写入同目录；
- `flush + fsync`；
- `os.replace`；
- 缓存先写临时文件，校验内容非空后再替换；
- 每个缓存保存长度、哈希或版本元数据；
- 加载损坏缓存时自动删除并重试。

### 5.2 最终 TXT 也应采用临时文件替换

[`engine/crawler.py:769-772`](engine/crawler.py:769) 直接写 `book.txt`。当阅读接口同时读取时，可能读到半截文件。

建议写入 `book.txt.tmp`，完成后 `os.replace`；大文件可采用分段写入临时文件后替换。

### 5.3 漫画书库存在读改写丢更新风险

[`engine/manga/download_manager.py:515-542`](engine/manga/download_manager.py:515) 对书库文件进行整体读、合并、写入，但缺少统一进程锁和原子替换；Flask 侧删除书库也有独立的裸读改写逻辑，见 [`app.py:3043-3053`](app.py:3043)。

并发下载任务完成时，后写入者可能覆盖先写入者的更新。

建议：

- 所有书库变更经过单一 repository；
- 使用进程内锁；
- 使用原子替换；
- 写入时带版本号或 `updated_at`，拒绝旧版本覆盖新版本；
- 多进程部署时使用 SQLite，而不是共享 JSON 文件。

### 5.4 图片下载器存在共享状态竞态

[`engine/manga/downloader.py:69-71`](engine/manga/downloader.py:69) 将当前章节和 URL 写入实例属性，随后 [`engine/manga/downloader.py:113-117`](engine/manga/downloader.py:113) 读取这些属性做图片后处理；但下载管理器支持并发下载，见 [`engine/manga/download_manager.py:447-457`](engine/manga/download_manager.py:447)。

两个线程可能互相覆盖 `_cur_ep/_cur_url`，导致需要按章节参数解密/还原的图片使用错误上下文。

建议将章节和 URL 作为 `_download()` 的显式参数传递，删除 `_cur_ep/_cur_url` 共享状态：

```python
return self._download(image_url, cp, timeout, chapter_id, image_url)
```

### 5.5 Fetcher 会话重建需要和在途请求协调

Fetcher 在重建会话时关闭旧 session，但其他线程可能正在使用旧 session。整书并发抓取和封锁切换同时发生时，可能产生连接重置或文件描述符错误。

建议：

- 每个 host 使用 session generation；
- 新 session 创建完成后再原子替换引用；
- 旧 session 只在引用计数归零后关闭；
- 或者让每个 worker 使用独立 session，避免共享 session 的关闭竞态。

### 5.6 重试缺少端到端 deadline

Fetcher 的单次重试有退避，但目录和详情 API 的完整调用链缺少明确总时限。某些路径可能在同步 Flask 请求中等待多轮请求和重试，造成线程长期占用。

建议每个外部操作都传递 deadline：

```python
deadline = monotonic() + 20
while monotonic() < deadline:
    ...
```

单次连接超时、读取超时和整个业务操作超时必须分开控制。

---

## 6. 性能与并发

### 6.1 搜索线程池存在嵌套和放大风险

搜索请求在 `app.py` 创建线程池，同时 `SearchWorker` 内部又维护线程池。根据配置，Web 端搜索并发上限为 64，见 [`engine/config.py:29-35`](engine/config.py:29)，多请求并发时可能产生大量等待线程。

建议选择一种模型：

- API 层只提交一个 SearchWorker 任务；或
- SearchWorker 负责整个批次，不在 API 层再次包线程池。

另外，对慢源应实现真正的取消或隔离，否则超时返回后底层请求仍可能占用线程池槽位。

### 6.2 `load_all()` 高频全量扫描磁盘

[`engine/source_mgr.py:30-45`](engine/source_mgr.py:30) 每次读取全部 JSON；`get_by_uid()` 又在此基础上遍历。源数量扩大后会产生重复 I/O。

建议引入带版本号的 `SourceRepository`：

- 启动时加载；
- 文件变更时刷新；
- CRUD 后更新内存索引；
- 需要多进程时使用 SQLite。

### 6.3 书库列表逐章 stat

书库列表路径逐章检查文件存在，而详情接口已使用目录索引。应统一生成章节状态索引，避免单书数千次 `stat` 调用。

### 6.4 自动代理获取不应阻塞用户请求

代理池为空时会同步拉取公共代理并验证，容易让用户搜索请求承担代理发现成本。建议代理池维护改成后台任务，用户请求只读取当前可用池；公共代理默认应关闭或必须显式启用。

---

## 7. 安全与合规补充问题

### 7.1 漫画代理接口的 URL 约束过弱

[`app.py:3434-3436`](app.py:3434) 只检查 URL 是否以 `http` 开头。除了 SSRF，还应限制：

- scheme 必须是 `http` 或 `https`；
- hostname 必须存在；
- 禁止内网和保留地址；
- 只能代理适配器返回的 URL；
- 限制响应大小、Content-Type 和下载时长。

### 7.2 封面 URL 拼入 innerHTML

漫画模板中封面 URL 直接插入 HTML 属性，而其他文本字段使用了转义函数。应使用 DOM 属性赋值：

```javascript
const img = document.createElement('img');
img.src = coverUrl;
```

或至少对 URL 做 HTML 属性转义并限制协议。

### 7.3 Android 签名口令不应明文写入构建脚本

`android_app/app/build.gradle` 中存在明文 `storePassword` 和 `keyAlias`。签名密码应移到本地 Gradle properties、CI secret 或签名服务，并立即轮换已经暴露的口令。

### 7.4 异常信息不应原样返回客户端

`app.py` 多处将异常类型和消息拼入 API 响应。生产环境应：

- 客户端只返回稳定错误码和 request ID；
- 服务端日志记录完整堆栈；
- 对 URL、文件路径、代理错误和源规则做脱敏。

### 7.5 反爬对抗和版权风险需要单独治理

系统包含 TLS 指纹模拟、UA 轮换、封锁检测、代理池和站点风控降级逻辑。技术上增强了可用性，但也提高了目标站点服务条款、版权内容批量抓取和第三方代理数据泄露的合规风险。

应至少补充：

- 目标站点 allowlist；
- robots/服务条款与授权记录；
- 速率和总量上限；
- 用户可配置的站点禁用策略；
- 公共代理默认关闭；
- 下载内容的生命周期和删除策略。

---

## 8. 测试与工程化评估

### 8.1 当前测试基线是合格的起点

审查中运行现有测试结果：

```text
56 passed, 1 skipped
```

核心 Python 文件的语法编译检查也通过。`tests/test_api.py` 已覆盖页面渲染、书源导入、搜索、任务和部分规则解析；`tests/test_manga.py` 覆盖了章节排序和基础模型。

### 8.2 但关键风险区域几乎没有回归保护

缺口包括：

- Fetcher 封锁检测；
- `Retry-After`；
- 会话重建；
- 代理池；
- AIMD 限速；
- CrawlTask 断点恢复；
- 损坏缓存恢复；
- DownloadManager 状态持久化；
- ImageDownloader 并发；
- 鉴权；
- SSRF；
- UID 路径穿越；
- 漫画 API 和书库并发更新。

### 8.3 依赖声明与实际使用不一致

[`requirements.txt`](requirements.txt) 只有 Flask、cloudscraper、lxml 和 jsonpath-ng，但代码实际还依赖或可选依赖 `requests`、`curl_cffi`、`playwright`、`opencc`、图像处理相关库等。所有依赖只使用 `>=`，没有 lockfile，导致新环境不可复现。

建议：

- 分拆 `requirements/base.txt`、`requirements/manga.txt`、`requirements/dev.txt`；
- 使用 `requirements.lock` 或 `uv.lock`/Poetry lock；
- 明确可选能力缺失时应该失败还是降级；
- 启动时输出依赖能力矩阵，而不是静默降级。

### 8.4 缺少 CI 和生产运行规范

当前没有看到完整 CI、类型检查、lint 规则或生产 WSGI 配置。`start.sh` 使用 Flask 开发服务器和 `nohup`，缺少：

- 进程守护；
- 优雅关闭；
- 自动重启；
- 健康检查；
- 日志轮转；
- 资源限制；
- 备份与恢复演练。

建议至少建立：

```text
ruff/flake8
mypy 或 pyright
pytest
pip-audit
bandit
Docker/launchd/systemd
```

并在 CI 中执行安全测试和最小启动测试。

---

## 9. 建议整改路线

### 第一阶段：安全止血，目标 1-2 天

1. 默认 host 改为 `127.0.0.1`；
2. 默认强制鉴权或首次启动初始化密码；
3. 统一 URLPolicy，阻断 SSRF；
4. UID 改为服务端生成并做路径边界校验；
5. 修复封面 URL 的 innerHTML 注入；
6. 移除并轮换 Android 明文签名口令；
7. 生产模式禁止 debug；
8. API 错误响应改为错误码 + request ID。

### 第二阶段：可靠性修复，目标 3-5 天

1. 将 `CrawlTask.crawl()` 全部包入 `try/except/finally`；
2. `_state.json`、`_chapters.json`、章节缓存、`book.txt` 统一原子写；
3. 漫画任务和书库 JSON 统一锁与原子替换；
4. 删除 `ImageDownloader` 的共享 `_cur_*` 状态；
5. Fetcher 会话重建采用 generation 或引用计数；
6. 为目录、详情、章节下载增加业务级 deadline；
7. 增加任务 watchdog 和心跳。

### 第三阶段：架构收敛，目标 1-2 周

1. 拆分 `app.py` 为 blueprint、service、repository；
2. 统一配置和状态枚举；
3. 统一小说/漫画任务生命周期；
4. 把缓存、书源、书库 JSON 访问集中到 repository；
5. 统一搜索分组、详情聚合和 TOC 单飞逻辑；
6. 漫画适配器改为显式注册或自发现；
7. 建立结构化日志和指标。

### 第四阶段：生产化，目标 2-4 周

1. JSON 持久化迁移到 SQLite，下载文件仍保留文件系统；
2. 使用生产 WSGI/ASGI 服务；
3. 增加 CI、lint、类型检查和依赖审计；
4. 增加安全回归测试和故障注入测试；
5. 增加备份、恢复和数据一致性校验；
6. 增加 `/healthz`、任务队列深度、源成功率、P95 延迟、失败率等指标；
7. 对每个来源维护可用性、授权和速率策略。

---

## 10. 最终判断

### 可以肯定的部分

- 功能范围明显超过普通爬虫脚本；
- 规则引擎、适配器、Fetcher 和下载管理器的基本方向合理；
- 已有缓存、限流、重试、健康检查、失败章节重试和离线测试；
- 当前测试基线能够证明主要 API 没有明显语法级或基础流程级故障。

### 不能接受的部分

- 默认开放监听且鉴权关闭；
- 用户可控 URL 缺少统一目标约束；
- 书源 UID 可影响落盘路径；
- 断点和下载状态的关键文件不是统一原子写；
- 复杂并发路径存在共享状态竞态；
- 依赖、CI、部署和日志体系没有达到长期无人值守要求。

### 一句话结论

**这是一套“功能很强、架构方向正确、但安全与可靠性尚未封顶”的个人级爬虫平台。修完安全止血和持久化/任务状态问题后，可以成为稳定的家庭局域网服务；在此之前，不应直接公网部署，也不应把当前状态当作生产级爬虫基础设施。**
