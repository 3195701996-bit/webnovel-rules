[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
# 超时不写在此处：pytest-timeout 属可选开发依赖，
# 未安装时 timeout= 会触发 Unknown config option。
# CI 已装该插件并通过 --timeout=120 显式启用。
filterwarnings =
    ignore::DeprecationWarning
